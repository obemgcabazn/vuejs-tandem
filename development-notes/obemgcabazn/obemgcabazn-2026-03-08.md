- Реализовал автоматическое обновление JWT-токена: функция `refreshTokens()` в auth store, утилита `apiFetch` с проактивным рефрешем за 30 сек до истечения и retry при 401
- Подключил `apiFetch` в ProfileView вместо локального fetch — теперь 401 на странице профиля не падает, токен обновляется автоматически
- Добавил валидацию формы логина/регистрации через `loginValidator.ts`: email (regex), пароль (строчная + прописная + цифра), имя (3–12 символов); ошибки показываются после blur, кнопка блокируется если форма невалидна
- Обновил компонент `AppInput`: добавил пропс `error`, стили ошибки (красная рамка + текст под полем), эмит `blur`
- Переделал навигацию в `App.vue`: фиксированный хедер в одну строку, тёмный фон с blur, ссылки с hover/active состояниями, кнопки «Войти» и «Выйти»
- Обновил дизайн страницы профиля